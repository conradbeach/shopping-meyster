var new_item_html = "<%= escape_javascript render('item', list: @list, item: @item) %>"

$("#item-<%= @item.id %>").replaceWith(new_item_html)

$("#total-cost").html("<%= @list.total_cost %>");
$("#items-remaining").html("<%= @list.items_remaining %>");
